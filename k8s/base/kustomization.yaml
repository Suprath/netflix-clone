apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - hpa.yaml

secretGenerator:
- name: tmdb-api-key
  literals:
  - TMDB_V3_API_KEY=

commonLabels:
  app.kubernetes.io/part-of: netflix-clone
